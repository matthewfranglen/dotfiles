#!/bin/bash

# Original Source: http://twitter.com/apag/statuses/1172677985
# Compresses the databases that firefox uses

cd ~/.mozilla/firefox/*.default && find -name '*.sqlite' | while read db
do
    echo 'vacuum;' | sqlite3 "$db"
done

# vim: set ai et sw=4 syntax=sh :
