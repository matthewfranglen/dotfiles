#!/usr/bin/env python
""" This will print the current python path in a manner suitable for assignment
    to PYTHONPATH. If PYTHONPATH is blank then this will be the system default
    python path. """

import sys

# The system path can contain a blank entry which is the current directory.
# When used in the PYTHONPATH environmental variable this should be translated
# to '.'
print ':'.join([p if p else '.' for p in sys.path])

# vim: set ai et sw=4 syntax=python :
