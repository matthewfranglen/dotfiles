---
- name: Install zsh
  apt: name=zsh state=present
  sudo: yes

# (could well be in vim/dependencies, but is aliased, so might as well be here)
# http://beyondgrep.com/
- name: Install ack-grep
  apt: name=ack-grep state=present
  sudo: yes

- name: Install zsh config folder
  synchronize: src=zsh dest=~/.zsh
  sudo: no

- name: Install zshrc
  synchronize: src=zshrc dest=~/.zshrc
  sudo: no

# vim: set ai et sw=2 syntax=yaml :
